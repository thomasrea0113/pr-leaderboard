/**
 * Useful in the default case of a switch statement to ensure that all possible values
 * of the switches value are handled
 * @param never any variable
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars, no-empty-function
export const neverReached = (never: never) => {};
