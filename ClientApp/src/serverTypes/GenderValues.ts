// the server is configured to serialize enums as there string value, so we need to explicity set it here as well
export enum GenderValuesEnum {
    Male = 'Male',
    Female = 'Female',
    Other = 'Other',
}

export default GenderValuesEnum;
